apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard-claim
  labels:
    app: dashboard-claim
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}