apiVersion: v1
kind: Service
metadata:
  labels:
    app: dashboard
  name: dashboard
spec:
  selector:
    app: dashboard
  ports:
  - name: "http"
    protocol: TCP
    port: 3000
    targetPort: 3000  
status:
  loadBalancer: {}